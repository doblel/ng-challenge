import { passwordValidator } from "./password.validator";
import { passwordMatchValidator } from "./password-match.validator";
import { passwordIncludesNameValidator } from "./password-includes-name.validator";

export default {
  passwordValidator,
  passwordMatchValidator,
  passwordIncludesNameValidator
}